services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DB_URL: mongodb://db:27017
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
      - ./db/init.js:/docker-entrypoint-initdb.d/init.js:ro
    restart: unless-stopped

volumes:
  mongo-data:
